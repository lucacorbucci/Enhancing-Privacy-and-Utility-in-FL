def configure_preferences(path) -> dict:
    """This function accepts the json preferences object and transforms it into
    preferences dicitonary. Using this function, we can easily convert between running
    the experiment from the json configuration or python dictionary. """
    pass